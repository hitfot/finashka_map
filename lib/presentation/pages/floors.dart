import 'package:flutter/material.dart';
import 'package:finashka_map/presentation/colors.dart';
import 'package:flutter_svg/flutter_svg.dart';

class Floor0 extends StatelessWidget {
  const Floor0({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
            title: const Text('Этаж 0'),
            backgroundColor: DarkThemeColors.background01
        ),
        backgroundColor: DarkThemeColors.background02,
        body: Center(
            child: Container(
              height: double.infinity,
              width: double.infinity,
              color: Colors.transparent,
              child: SvgPicture.asset(
                'assets/map/demo0.svg',
                color: DarkThemeColors.background03,
                width: 100,),
            )
        )
    );
  }
}

class Floor1 extends StatelessWidget {
  const Floor1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
            title: const Text('Этаж 1'),
            backgroundColor: DarkThemeColors.background01
        ),
        backgroundColor: DarkThemeColors.background02,
        body: Center(
            child: Container(
              height: double.infinity,
              width: double.infinity,
              color: Colors.transparent,
              child: SvgPicture.asset(
                'assets/map/demo0.svg',
                color: DarkThemeColors.background03,
                width: 100,),
            )
        )
    );
  }
}

class Floor2 extends StatelessWidget {
  const Floor2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
            title: const Text('Этаж 2'),
            backgroundColor: DarkThemeColors.background01
        ),
        backgroundColor: DarkThemeColors.background02,
        body: Center(
            child: Container(
              height: double.infinity,
              width: double.infinity,
              color: Colors.transparent,
              child: SvgPicture.asset(
                'assets/map/demo0.svg',
                color: DarkThemeColors.background03,
                width: 100,),
            )
        )
    );
  }
}

class Floor3 extends StatelessWidget {
  const Floor3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
            title: const Text('Этаж 3'),
            backgroundColor: DarkThemeColors.background01
        ),
        backgroundColor: DarkThemeColors.background02,
        body: Center(
            child: Container(
              height: double.infinity,
              width: double.infinity,
              color: Colors.transparent,
              child: SvgPicture.asset(
                'assets/map/demo0.svg',
                color: DarkThemeColors.background03,
                width: 100,),
            )
        )
    );
  }
}

class Floor4 extends StatelessWidget {
  const Floor4({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
            title: const Text('Этаж 4'),
            backgroundColor: DarkThemeColors.background01
        ),
        backgroundColor: DarkThemeColors.background02,
        body: Center(
            child: Container(
              height: double.infinity,
              width: double.infinity,
              color: Colors.transparent,
              child: SvgPicture.asset(
                'assets/map/demo0.svg',
                color: DarkThemeColors.background03,
                width: 100,),
            )
        )
    );
  }
}

class Floor5 extends StatelessWidget {
  const Floor5({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
            title: const Text('Этаж 5'),
            backgroundColor: DarkThemeColors.background01
        ),
        backgroundColor: DarkThemeColors.background02,
        body: Center(
            child: Container(
              height: double.infinity,
              width: double.infinity,
              color: Colors.transparent,
              child: SvgPicture.asset(
                'assets/map/demo0.svg',
                color: DarkThemeColors.background03,
                width: 100,),
            )
        )
    );
  }
}